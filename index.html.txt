<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TrendHire - AI Career Intelligence</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.9.1/chart.min.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: #333;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .header {
            text-align: center;
            color: white;
            margin-bottom: 40px;
        }
        
        .header h1 {
            font-size: 3rem;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }
        
        .header p {
            font-size: 1.2rem;
            opacity: 0.9;
        }
        
        .demo-section {
            background: white;
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }
        
        .demo-section:hover {
            transform: translateY(-5px);
        }
        
        .section-title {
            font-size: 1.8rem;
            margin-bottom: 20px;
            color: #333;
            border-bottom: 3px solid #667eea;
            padding-bottom: 10px;
        }
        
        .api-test {
            background: #f8f9fa;
            border-radius: 10px;
            padding: 20px;
            margin: 20px 0;
        }
        
        .btn {
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1rem;
            transition: all 0.3s ease;
            margin: 10px 5px;
        }
        
        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }
        
        .results {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 15px;
            margin-top: 15px;
            border-left: 4px solid #667eea;
        }
        
        .chart-container {
            width: 100%;
            height: 400px;
            margin: 20px 0;
        }
        
        .loading {
            display: none;
            text-align: center;
            color: #667eea;
            font-style: italic;
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin: 20px 0;
        }
        
        .stat-card {
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
        }
        
        .stat-number {
            font-size: 2rem;
            font-weight: bold;
            margin-bottom: 5px;
        }
        
        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin: 30px 0;
        }
        
        .feature-card {
            background: white;
            border-radius: 10px;
            padding: 20px;
            text-align: center;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }
        
        .feature-card:hover {
            transform: translateY(-5px);
        }
        
        .feature-icon {
            font-size: 3rem;
            margin-bottom: 15px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🚀 TrendHire</h1>
            <p>Discover the Future of Hiring—Before It Happens</p>
        </div>

        <div class="demo-section">
            <h2 class="section-title">📈 Live API Demo</h2>
            <p>TrendHire API is running live on Render. Test our real-time endpoints:</p>
            
            <div class="api-test">
                <button class="btn" onclick="testTrendingJobs()">🔍 Get Trending Jobs</button>
                <button class="btn" onclick="testSkillsAnalysis()">🧠 Analyze Skills</button>
                <button class="btn" onclick="testHealthCheck()">💊 Health Check</button>
                
                <div id="loading" class="loading">Loading real-time data...</div>
                <div id="apiResults" class="results" style="display: none;"></div>
            </div>
        </div>

        <div class="demo-section">
            <h2 class="section-title">📊 Trending Job Market Insights</h2>
            <div class="chart-container">
                <canvas id="trendChart"></canvas>
            </div>
        </div>

        <div class="demo-section">
            <h2 class="section-title">🎯 Key Metrics</h2>
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-number">2,847</div>
                    <div>Active Job Postings</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">340%</div>
                    <div>AI Job Growth</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">$158K</div>
                    <div>Avg AI Engineer Salary</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">24/7</div>
                    <div>Real-time Monitoring</div>
                </div>
            </div>
        </div>

        <div class="demo-section">
            <h2 class="section-title">🛠️ Bright Data Integration</h2>
            <div class="features">
                <div class="feature-card">
                    <div class="feature-icon">👁️</div>
                    <h3>Discover</h3>
                    <p>Find relevant content across Reddit, Dev.to, job boards, and learning communities</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">🔐</div>
                    <h3>Access</h3>
                    <p>Navigate complex websites, paywalls, and member-only sections seamlessly</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">📊</div>
                    <h3>Extract</h3>
                    <p>Pull structured, real-time data on jobs, skills, salaries, and courses at scale</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">🤖</div>
                    <h3>Interact</h3>
                    <p>Engage with JavaScript-rendered pages and dynamic content like a human</p>
                </div>
            </div>
        </div>

        <div class="demo-section">
            <h2 class="section-title">🏆 Why TrendHire Wins</h2>
            <ul style="font-size: 1.1rem; line-height: 1.8;">
                <li><strong>Unique:</strong> Goes beyond scraping—predicts, personalizes, and guides career decisions</li>
                <li><strong>Useful:</strong> Job seekers, universities, and HR teams need this intelligence today</li>
                <li><strong>Scalable:</strong> From SaaS to API to LMS plugins—ready for any integration</li>
                <li><strong>Real:</strong> Live API running with Bright Data MCP integration</li>
            </ul>
        </div>
    </div>

    <script>
        const API_BASE = 'https://trendhire.onrender.com';
        
        async function testTrendingJobs() {
            showLoading();
            try {
                const response = await fetch(`${API_BASE}/trending-jobs`);
                const data = await response.json();
                showResults('Trending Jobs Data', JSON.stringify(data, null, 2));
                updateChart(data.trending_jobs);
            } catch (error) {
                showResults('Error', 'Failed to fetch trending jobs: ' + error.message);
            }
        }
        
        async function testSkillsAnalysis() {
            showLoading();
            try {
                const response = await fetch(`${API_BASE}/skills-analysis/Python,React,SQL`);
                const data = await response.json();
                showResults('Skills Analysis Result', JSON.stringify(data, null, 2));
            } catch (error) {
                showResults('Error', 'Failed to analyze skills: ' + error.message);
            }
        }
        
        async function testHealthCheck() {
            showLoading();
            try {
                const response = await fetch(`${API_BASE}/health`);
                const data = await response.json();
                showResults('Health Check', JSON.stringify(data, null, 2));
            } catch (error) {
                showResults('Error', 'Failed to check health: ' + error.message);
            }
        }
        
        function showLoading() {
            document.getElementById('loading').style.display = 'block';
            document.getElementById('apiResults').style.display = 'none';
        }
        
        function showResults(title, content) {
            document.getElementById('loading').style.display = 'none';
            const resultsDiv = document.getElementById('apiResults');
            resultsDiv.innerHTML = `<h4>${title}</h4><pre>${content}</pre>`;
            resultsDiv.style.display = 'block';
        }
        
        function updateChart(jobData) {
            const ctx = document.getElementById('trendChart').getContext('2d');
            new Chart(ctx, {
                type: 'bar',
                data: {
                    labels: jobData.map(job => job.title),
                    datasets: [{
                        label: 'Growth Rate (%)',
                        data: jobData.map(job => job.growth),
                        backgroundColor: [
                            'rgba(102, 126, 234, 0.8)',
                            'rgba(118, 75, 162, 0.8)',
                            'rgba(255, 99, 132, 0.8)',
                            'rgba(54, 162, 235, 0.8)',
                            'rgba(255, 206, 86, 0.8)'
                        ],
                        borderColor: [
                            'rgba(102, 126, 234, 1)',
                            'rgba(118, 75, 162, 1)',
                            'rgba(255, 99, 132, 1)',
                            'rgba(54, 162, 235, 1)',
                            'rgba(255, 206, 86, 1)'
                        ],
                        borderWidth: 2
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    scales: {
                        y: {
                            beginAtZero: true,
                            title: {
                                display: true,
                                text: 'Growth Rate (%)'
                            }
                        },
                        x: {
                            title: {
                                display: true,
                                text: 'Job Titles'
                            }
                        }
                    },
                    plugins: {
                        title: {
                            display: true,
                            text: 'AI Job Market Growth Trends'
                        }
                    }
                }
            });
        }
        
        // Initialize chart with default data
        window.onload = function() {
            testTrendingJobs();
        };
    </script>
</body>
</html>